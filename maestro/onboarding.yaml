# Run: APP_ID=ai.krishisaathi.app maestro test maestro/onboarding.yaml
# Prefer production-install.yaml for full post-install checks.
appId: ${APP_ID}
---
- launchApp
- assertVisible: "Home|घर"
- assertVisible:
    id: "home-weather-card"
